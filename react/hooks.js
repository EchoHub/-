/**
 * @name 自定义hook实现
 */

/**
 * @name 防抖
 * @param {*} fn 
 * @param {*} wait 
 */
function useDebounce(fn, wait) {
 
}

/**
 * @name 截流
 * @param {*} fn 
 * @param {*} delay 
 */
function useThrottle(fn, delay) {

}